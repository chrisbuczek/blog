# LAOTs State Management

Recently I saw Anton Mikhailov talking how he and his team designed code structure in a game called Dreams. In this article I will show what LAOT's (Large Arrays of Things) are about.

## The basics

The most popular design that every programmer knows is Object-Oriented Programming. Let's see a simple example.

```typescript filename="OOP example" copy
class Object
```

In the example above I've created two functions: getFirst() and getFirstWithGenerics().
They do the same thing, but in getFirstWithGenerics we pass a type. We do this by using **\<your_type_here\>** when we call the function.
