# Maxx Burgers Hobby Project

## The inspiration

When I was visting fast food chains like McDonald's, KFC or Max Burgers I've always been intriguied by the order apps on those machines with big touch screens.
Thankfully Max Burgers chain has a <a style={{ textDecoration: "underline"}}href={'https://order.maxburgers.com'}>website</a> that is very similar to their app on order machines. I've always wondered how it works under the hood and I've decided to reacte this exact app by myself.

```js filename="PromiseExample.js" copy
new Promise((resolve) => {
  console.log(1);
  resolve(2);
}).then((result) => console.log(result));

console.log(3);
```
