# How JWT Works

Short explanation of how Json Web Token works using Node.js and jsonwebtoken library.

## The Basics

## Generating the token

```js filename="tokenGenerate.ts" copy
import jwt from "jsonwebtoken";

const generateToken = (id: string) => {
  return jwt.sign({ id }, process.env.JWT_SECRET_KEY as string, { expiresIn: "30d" });
};

export default generateToken;
```

## Middleware auth (protect)
