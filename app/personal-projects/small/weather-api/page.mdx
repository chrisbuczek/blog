# Weather API with Redis

Stack: Node.js, Redis,

## Overview

I have created this project to learn node.js as API server, Redis for caching weather responses and usage of 3rd party weather service - [visualcrossing API](https://www.visualcrossing.com/weather-api/)

![Weather API screenshot](/weather-api.png)

```js filename="CodeExample.ts" copy
new Promise((resolve) => {
  console.log(1);
  resolve(2);
}).then((result) => console.log(result));

console.log(3);
```
